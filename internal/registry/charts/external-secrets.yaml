chart: external-secrets
mappings:
  - rule: RES001
    template: templates/deployment.yaml
    candidates:
      - values_base: resources
        summary: Populate External Secrets controller resource limits through the top-level resources subtree.
  - rule: RES002
    template: templates/deployment.yaml
    candidates:
      - values_base: resources
        summary: Populate External Secrets controller resource requests through the top-level resources subtree.
