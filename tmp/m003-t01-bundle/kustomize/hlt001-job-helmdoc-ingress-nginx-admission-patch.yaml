# Generated by helmdoc fix.
# File: kustomize/hlt001-job-helmdoc-ingress-nginx-admission-patch.yaml
# Apply this file from the generated kustomize/ overlay after rendering the chart manifests.
# Target resource: Job/helmdoc-ingress-nginx-admission-patch
# Target namespace: default
# Provenance: HLT001 Job/helmdoc-ingress-nginx-admission-patch @ templates/admission-webhooks/job-patch/job-patchWebhook.yaml
# target container: patch
# planned patch: Add a conservative livenessProbe directly to the rendered workload container.
# rule detail: container "patch" in Job/helmdoc-ingress-nginx-admission-patch has no livenessProbe.
apiVersion: batch/v1
kind: Job
metadata:
  name: helmdoc-ingress-nginx-admission-patch
  namespace: default
spec:
  template:
    spec:
      containers:
      - livenessProbe:
          exec:
            command:
            - /kube-webhook-certgen
            - --help
          failureThreshold: 3
          initialDelaySeconds: 5
          periodSeconds: 10
          successThreshold: 1
          timeoutSeconds: 1
        name: patch
