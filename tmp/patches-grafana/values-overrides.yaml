# Generated by helmdoc fix.
# File: values-overrides.yaml
# Apply this file with your Helm values workflow, for example: helm upgrade --install RELEASE CHART -f values-overrides.yaml
# These overrides contain only findings helmdoc could map to chart values.
# Provenance:
# - NET001 Deployment/helmdoc-grafana @ templates/deployment.yaml -> imageRenderer.networkPolicy.enabled
# planned fix: Enable the only exposed chart networkPolicy knob.
# rule detail: Deployment/helmdoc-grafana is rendered in namespace "default", but no NetworkPolicy is rendered for that namespace.
imageRenderer:
  networkPolicy:
    enabled: true
