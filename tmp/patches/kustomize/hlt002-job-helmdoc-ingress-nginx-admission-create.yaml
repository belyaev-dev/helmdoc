# Generated by helmdoc fix.
# File: kustomize/hlt002-job-helmdoc-ingress-nginx-admission-create.yaml
# Apply this file from the generated kustomize/ overlay after rendering the chart manifests.
# Target resource: Job/helmdoc-ingress-nginx-admission-create
# Target namespace: default
# Provenance: HLT002 Job/helmdoc-ingress-nginx-admission-create @ templates/admission-webhooks/job-patch/job-createSecret.yaml
# target container: create
# planned patch: Add a conservative readinessProbe directly to the rendered workload container.
# rule detail: container "create" in Job/helmdoc-ingress-nginx-admission-create has no readinessProbe.
apiVersion: batch/v1
kind: Job
metadata:
  name: helmdoc-ingress-nginx-admission-create
  namespace: default
spec:
  template:
    spec:
      containers:
      - name: create
        readinessProbe:
          exec:
            command:
            - /kube-webhook-certgen
            - --help
          failureThreshold: 1
          periodSeconds: 5
          successThreshold: 1
          timeoutSeconds: 1
