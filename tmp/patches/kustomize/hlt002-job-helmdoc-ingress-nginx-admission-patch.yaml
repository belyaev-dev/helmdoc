# Generated by helmdoc fix.
# File: kustomize/hlt002-job-helmdoc-ingress-nginx-admission-patch.yaml
# Apply this file from the generated kustomize/ overlay after rendering the chart manifests.
# Target resource: Job/helmdoc-ingress-nginx-admission-patch
# Target namespace: default
# Provenance: HLT002 Job/helmdoc-ingress-nginx-admission-patch @ templates/admission-webhooks/job-patch/job-patchWebhook.yaml
# target container: patch
# planned patch: Add a conservative readinessProbe directly to the rendered workload container.
# rule detail: container "patch" in Job/helmdoc-ingress-nginx-admission-patch has no readinessProbe.
apiVersion: batch/v1
kind: Job
metadata:
  name: helmdoc-ingress-nginx-admission-patch
  namespace: default
spec:
  template:
    spec:
      containers:
      - name: patch
        readinessProbe:
          exec:
            command:
            - /kube-webhook-certgen
            - --help
          failureThreshold: 1
          periodSeconds: 5
          successThreshold: 1
          timeoutSeconds: 1
